-- NOT NULL | NULL
-- PRIMARY KEY(COLUMN)
-- DEFAULT DEFAULT_VAL


-- CHECK {{{
CREATE TABLE TB(
  COL1,
  COL2,
  CHECK(
    CASE COL
      WHEN CONDITION1 THEN RESULT1
      WHEN CONDITION2 THEN RESULT2
      ELSE RESULT3
    END
  )
)
;
-- }}}