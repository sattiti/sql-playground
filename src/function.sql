-- COUNT(*)
-- COUNT(COLUMN)
-- MIN(COLUMN)
-- MAX(COLUMN)
-- AVG(COLUMN)
-- SUM(COLUMN)
-- IFNULL(COLUMN, 0)

SELECT COUNT(COL1) AS NEW_COL1
  FROM
    TB
  WHERE
    CONSTRAINT_EXPRESSION
  GROUP BY
    COL
;
